import { describe, test } from "vitest";

describe('TodoList', () => {
  test.todo('render component as done')
  test.todo('render component as pending')
  
  test.todo('emit removal of item')
  test.todo('emit edit of item information')
  test.todo('emit edit of item state')
});
